{% extends 'base.html.twig' %}

{% block title %}Add New City{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4 text-center">Create New City</h1>

        <div class="card shadow-sm">
            <div class="card-body">
                {{ include('city/_form.html.twig') }}

                <div class="mt-3">
                    <a href="{{ path('app_city_index') }}" class="btn btn-secondary">Back to List</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
